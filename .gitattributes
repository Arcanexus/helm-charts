
# Normalize text files to LF in the repository; allow OS-native checkouts
* text=auto

# Explicit text file types (stored normalized as LF in the repo)
*.yaml text
*.yml text
*.json text
*.md text
*.txt text
*.xml text
*.tpl text

# Shell scripts and templates: force LF on checkout (useful for Unix tooling)
*.sh text eol=lf
*.bash text eol=lf
*.zsh text eol=lf

# Windows-specific scripts: prefer CRLF on checkout
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binary files — do not perform EOL conversion
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
*.pdf binary
*.zip binary
*.tar.gz binary
*.tgz binary

# EOF
