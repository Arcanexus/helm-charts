apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-prometheus
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-prometheus
    helm.sh/chart: {{ include "app.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.prometheus.httpport }}
    targetPort: {{ .Values.prometheus.httpport }}
  selector:
    app: {{ include "app.name" . }}-{{ .Release.Name }}-prometheus
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-pushgateway
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-pushgateway
    helm.sh/chart: {{ include "app.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.pushgateway.httpport }}
    targetPort: {{ .Values.pushgateway.httpport }}
  selector:
    app: {{ include "app.name" . }}-{{ .Release.Name }}-pushgateway
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-grafana
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-grafana
    helm.sh/chart: {{ include "app.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.grafana.httpport }}
    targetPort: {{ .Values.grafana.httpport }}
  selector:
    app: {{ include "app.name" . }}-{{ .Release.Name }}-grafana
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-syslog-ng-udp
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-syslog-ng
    helm.sh/chart: {{ include "app.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
  - name: udp
    port: {{ index .Values "syslog-ng" "udpport" }}
    targetPort: {{ index .Values "syslog-ng" "udpport" }}
    protocol: UDP
  selector:
    app: {{ include "app.name" . }}-{{ .Release.Name }}-syslog-ng
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-syslog-ng-tcp
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-syslog-ng
    helm.sh/chart: {{ include "app.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
  - name: tcp
    port: {{ index .Values "syslog-ng" "tcpport" }}
    targetPort: {{ index .Values "syslog-ng" "tcpport" }}
    protocol: TCP
  selector:
    app: {{ include "app.name" . }}-{{ .Release.Name }}-syslog-ng
