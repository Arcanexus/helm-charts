apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app.name" . }}
spec:
  replicas: {{ .Values.tesladash.replicas }}
  #minReadySeconds: 10
  selector:
    matchLabels:
      app: {{ include "app.name" . }}
  revisionHistoryLimit: 0
  template:
    metadata:
      labels:
        app: {{ include "app.name" . }}
        app.kubernetes.io/name: {{ include "app.name" . }}
        helm.sh/chart: {{ include "app.chart" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
      annotations:
        checksum/bookmarks: {{ .Values.tesladash.bookmarks | toJson | sha256sum | trunc 62 }}

    spec:
      dnsPolicy: ClusterFirst
      enableServiceLinks: true
      containers:
        - name: {{ include "app.name" . }}
          image: {{ .Values.tesladash.image }}
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.tesladash.port }}
              name: web
          volumeMounts:
            # - name: {{ include "app.name" . }}-{{ .Values.tesladash.volumes.bookmarks.name }}
            #   mountPath: {{ .Values.tesladash.volumes.bookmarks.containerMountPath }}
            - name: {{ include "app.name" . }}-{{ .Values.tesladash.volumes.images.name }}
              mountPath: {{ .Values.tesladash.volumes.images.containerMountPath }}
            - name: {{ include "app.name" . }}-bookmarks
              mountPath: /app/public/bookmarks.json
              subPath: bookmarks.json
          env:
          - name: TZ
            value: "Europe/Paris"
          {{- if .Values.tesladash.environment }}
          {{- range $key, $value := .Values.tesladash.environment }}
          - name: {{ $key }}
            value: {{ $value | quote }}
          {{- end }}
          {{- end }}
      volumes:
      # - name: {{ include "app.name" . }}-{{ .Values.tesladash.volumes.bookmarks.name }}
      #   nfs:
      #     server: {{ .Values.tesladash.volumes.bookmarks.server }}
      #     path: {{ .Values.tesladash.volumes.bookmarks.path }}
      - name: {{ include "app.name" . }}-{{ .Values.tesladash.volumes.images.name }}
        nfs:
          server: {{ .Values.tesladash.volumes.images.server }}
          path: {{ .Values.tesladash.volumes.images.path }}
      - name: {{ include "app.name" . }}-bookmarks
        configMap:
          name: tesladash-bookmarks
          items:
            - key: bookmarks.json
              path: bookmarks.json