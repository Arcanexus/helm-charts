{{- if .Values.omnitool.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "app.name" . }}-omnitoolingress
  annotations:
    {{- if .Values.omnitool.ingress.annotations }}
    {{- toYaml .Values.omnitool.ingress.annotations | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: traefik
  {{- if .Values.omnitool.ingress.enabled }}
  tls:
    - hosts:
        - {{ .Values.omnitool.ingress.host }}
      secretName: {{ include "app.name" . }}-omnitool-tls
  {{- end }}
  rules:
    - host: {{ .Values.omnitool.ingress.host }}
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ .Release.Name }}-service
                port:
                  number: {{ .Values.omnitool.port }}
{{- end }}